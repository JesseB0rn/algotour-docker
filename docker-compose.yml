services:
  qgis:
    build: .
    container_name: qgis-model-firebase
    volumes:
      - /etc/firebase.json:/root/firebase.json
      - /etc/riskmaps:/root/riskmaps
      - /etc/dem:/root/dem
      - ./config.py:/root/config.py
      - ./routes:/root/routes
